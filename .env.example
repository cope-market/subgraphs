# Subgraph Studio deploy key. One key covers every subgraph on the account, so the same value
# deploys both erc4626-vault-arc and cope-market-arc.
# Studio: https://thegraph.com/studio -> the subgraph -> "AUTH & DEPLOY".
GRAPH_DEPLOY_KEY=
